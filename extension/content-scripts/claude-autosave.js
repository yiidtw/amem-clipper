// amem — Claude.ai autosave placeholder.
// Day 1-2: stub only. Conversation extraction lands in Day 3+.

(() => {
  if (window.__amemClaudeLoaded) return;
  window.__amemClaudeLoaded = true;
  console.debug('[amem:claude] content script loaded');
})();
