// amem — Gemini autosave placeholder.
// Day 1-2: stub only. Conversation extraction lands in Day 3+.

(() => {
  if (window.__amemGeminiLoaded) return;
  window.__amemGeminiLoaded = true;
  console.debug('[amem:gemini] content script loaded');
})();
